<%= form_with(model: searchterm, local: true) do |form| %>
  <% if searchterm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(searchterm.errors.count, "error") %> prohibited this searchterm from being saved:</h2>

      <ul>
      <% searchterm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :search_term %>
    <%= form.text_field :search_term %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
